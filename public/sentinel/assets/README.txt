Sentinel AI — site assets
==========================

Drop your images in THIS folder using these exact names, then tell me they're in
and I'll wire them into the pages (logo in the nav/hero + favicon, screenshot in
the landing page as a product showcase).

1) LOGO  ->  sentinel-logo.png
   - Square-ish, transparent background ideal (PNG or SVG).
   - Used in the top nav, and as the browser favicon.
   - (If you have an .svg too, drop sentinel-logo.svg — even crisper.)

2) UI SCREENSHOT  ->  sentinel-screenshot.png
   - A clean shot of the app auditing some code (the findings panel looks great).
   - Wide/landscape is best (it'll sit in a framed showcase under the hero).
   - PNG preferred; high-res is fine, I'll size it responsively.

Optional extras (drop if you have them, same folder):
   - sentinel-screenshot-2.png, sentinel-screenshot-3.png  (for a small gallery)
   - og-image.png  (1200x630 social-share preview image)

Final URLs once deployed:
   https://camlabs.ai/sentinel/assets/sentinel-logo.png
   https://camlabs.ai/sentinel/assets/sentinel-screenshot.png
